# 小柏 Agent 面试手册

> **使用说明**：本手册为面试准备编写，覆盖项目架构、核心模块、调试故事和面试Q&A。目标是让你能深入理解这个项目，在面试中自信地讨论技术细节和工程决策。
---
## 一、项目执行全流程（一条消息的完整旅程）
以用户在微信发送"今天心情不错"为例，完整追踪代码路径：
### 1. 消息接入
```
用户微信发消息 → iLink Bot API → 微信通道接收
**代码位置**：`main.py` daemon 模式下的微信轮询循环，接收到消息后调用 `CommandDispatcher.dispatch()`
### 2. 命令分发
```python
# src/companion/command_dispatcher.py 第47行
async def dispatch(self, message: str) -> str:
    content = message.strip()
    # 检查是否为已知命令（日报/周报/月报/情绪/模式/统计）
    if content == "日报": ...
    # 未知消息 → 对话流程
    if self.task_mgr:
        await self.task_mgr.detect_task_list(content)  # 检测是否在列任务清单
    response = await self.handler.handle_message(content)  # 核心对话流程
    if self.task_mgr and self.llm:
        self.task_mgr.detect_task_completion(content, self.llm)  # 检测任务完成
    return response
### 3. ConversationHandler.handle_message() — 核心7步流程
**文件**：`src/companion/handler.py` 第162-253行
Step 1: 保存用户消息到数据库
  → ConversationMessage(session_id, role="user", content, timestamp)
  → await self.memory.save_message(user_msg)  # 写入 conversations 表
Step 1.5: 检测任务移动指令（在LLM回复前执行）
  → _detect_task_move(user_message)  # 正则检测"挪到明天/后天"
  → 命中则执行数据库 UPDATE，返回 action_result
Step 2: 检索相关记忆
  → _get_known_facts()       # 从 facts 表读取，分层过滤+去重
  → _get_today_tasks()       # 从 tasks 表读取今日任务
  → _get_related_memories()  # EmbeddingCache 语义搜索
Step 3: 构建系统提示
  → SYSTEM_PROMPT_TEMPLATE.format(
      companion_name, user_name, current_date, current_time,
      known_facts, related_memories, today_tasks, action_result
    )
Step 4: 调用 LLM
  → temperature = get_temperature(user_message)  # 0.7(闲聊) 或 0.4(学习)
  → await self.llm.chat(messages, temperature=temperature)
Step 5: 清理 LLM 回复中的任务标记
  → re.sub(r'\[TASKS_DETECTED\].*?\[/TASKS_DETECTED\]', '', response)
Step 6: 保存助手回复到数据库
  → await self.memory.save_message(assistant_msg)
Step 7: 异步提取信息（fire and forget）
  → extractor.extract(user_msg) → (facts, emotion, topics)
  → 保存到 facts/emotions/associations 表
### 4. SSE 流式输出路径
**文件**：`src/api/chat.py` → `POST /api/chat/stream` → `handler.stream_handle_message()`
前端 JS 通过 `fetch('/api/chat/stream')` 发起 SSE 请求，读取 `resp.body.getReader()` 逐块解析 `data: {"type": "chunk", "content": "..."}` 格式的消息。
### 5. 数据存储最终状态
消息处理完成后，数据库中新增记录：
- `conversations` 表：+1 条 user 消息 + 1 条 assistant 消息
- `facts` 表：提取的事实（如果有）
- `emotions` 表：情绪记录（如果不是 neutral）
- `associations` 表：话题标签关联
## 二、分层架构详解
### 2.1 多通道接入层
系统支持三种接入通道，共享同一套对话记忆：
| 通道 | 实现 | 特点 |
|------|------|------|
| **微信** | iLink Bot API | 扫码登录，实时消息 |
| **飞书** | 飞书开放平台 webhook | 企业级 IM |
| **Web** | FastAPI + SSE | 浏览器直接访问 `/chat` |
**关键设计**：所有通道最终都调用同一个 `CommandDispatcher.dispatch()`，保证行为一致。
### 2.2 命令分发层
**文件**：`src/companion/command_dispatcher.py`
采用**中介者模式（Mediator Pattern）**，统一处理来自不同通道的命令：
class CommandDispatcher:
    KNOWN_COMMANDS = {"日报", "周报", "月报", "情绪", "模式", "统计"}
    
    async def dispatch(self, message: str) -> str:
        # 已知命令 → 专用处理器
        if content == "日报": return daily_report.generate()
        # 未知消息 → 对话流程（含任务检测）
        await self.task_mgr.detect_task_list(content)
        response = await self.handler.handle_message(content)
        self.task_mgr.detect_task_completion(content, self.llm)
        return response
**设计优势**：消除各通道重复的命令分发逻辑，新增命令只需在 `KNOWN_COMMANDS` 和 `dispatch()` 中添加。
### 2.3 Agent核心层（ConversationHandler）
**文件**：`src/companion/handler.py`
这是整个系统的核心，负责：
1. **对话管理**：session 管理、消息保存
2. **上下文构建**：从记忆系统检索相关信息，注入系统提示
3. **LLM 调用**：非流式 `chat()` 和流式 `stream_chat()`
4. **信息提取**：fire-and-forget 模式异步提取事实、情绪、话题
5. **自适应温度**：根据内容类型动态调整 temperature
6. **任务管理**：检测任务列表、任务完成、任务移动
**关键属性**：
class ConversationHandler:
    TEMPERATURE_NORMAL = 0.7    # 闲聊温度
    TEMPERATURE_LEARNING = 0.4  # 学习温度（更稳定）
    LEARNING_KEYWORDS = ["学了", "学完", "学习", "看书", "做了", "理解了", ...]
### 2.4 记忆系统层（5+1层记忆架构）
**文件**：`src/memory/database.py`, `src/memory/base.py`
| 层级 | 内容 | 表名 | 说明 |
|------|------|------|------|
| Layer 0 | 原始对话存档 | `conversations` | 全量存储，带时间戳 |
| Layer 1 | 提取的结构化事实 | `facts` | 8种类型（person/preference/ability/goal/habit/event/commitment/opinion） |
| Layer 2 | 情绪时间线 | `emotions` | 8种情绪类型，带强度和触发原因 |
| Layer 3 | 关联索引 | `associations` | 关键词 → 消息ID/事实ID 的映射 |
| Layer 4 | 任务管理 | `tasks` | 用户任务 + 系统内置任务 |
| +1 | 学习记录 | `learning_log` | 学习内容、理解程度、关联主题 |
**数据库规格**：
- **8 张表**：conversations, facts, emotions, associations, tasks, summaries, embeddings, learning_log
- **12 个索引**：idx_conv_session, idx_conv_timestamp, idx_conv_role, idx_facts_type, idx_facts_subject, idx_facts_active, idx_emotions_timestamp, idx_assoc_keyword, idx_summaries_date, idx_summaries_type, idx_embeddings_entity, idx_embeddings_type, idx_learning_topic, idx_learning_created（实际有14个，核心12个）
- **SQLite WAL 模式**：支持并发读写
- **aiosqlite**：异步数据库操作
**Fact 分层过滤策略**（`_get_known_facts()`）：
- 稳定画像（preference/opinion/habit/person/goal）：全保留
- 临时事件（event/commitment）：只保留最近7天
- 7天前的旧事件：按 subject 去重，只保留最新1条
- 效果：152条 → 约30-40条，减少 prompt 噪音
### 2.5 LLM抽象层
**文件**：`src/llm/base.py`, `src/llm/factory.py`
class LLMProvider(ABC):
    """LLM 提供者抽象基类"""
    async def chat(messages, temperature, max_tokens) -> ChatResponse
    async def stream_chat(messages, temperature, max_tokens) -> AsyncGenerator[str]
    async def embed(text) -> List[float]  # 向量嵌入
    async def health_check() -> bool
**工厂模式**：
def create_llm_provider(config: LLMConfig):
    if config.provider == "ollama": return OllamaProvider(...)
    elif config.provider == "openai": return OpenAIProvider(...)
def create_embedding_provider(config: LLMConfig):
    """embedding 固定用 Ollama（云端 API 通常不支持）"""
    return OllamaProvider(...)
**两个 Provider**：
| Provider | 底层库 | 用途 | 特点 |
|----------|--------|------|------|
| OllamaProvider | httpx | 本地推理 + embedding | 连接本地 Docker Ollama |
| OpenAIProvider | openai SDK | 云端 API | 支持 Mimo 等 OpenAI 兼容 API |
**重试机制**：`src/llm/retry.py` — 指数退避重试
async def retry_with_backoff(func, max_retries=3, base_delay=1.0, max_delay=30.0):
    # delay = min(base_delay * (2 ** attempt), max_delay)
    # 1s → 2s → 4s → 8s → 16s → 30s(上限)
### 2.6 基础设施层
**容错层**：`src/llm/resilience.py` — 三层防护（兜底+熔断+缓存）

```
LLMResilience
├── CircuitBreaker  — 连续失败5次 → OPEN（熔断）→ 60秒冷却 → HALF_OPEN（试探）→ 恢复
├── ResponseCache   — LRU + TTL缓存，相同query返回缓存（100条，1小时过期）
└── Fallback        — LLM失败时返回轮询友好提示，不让用户无限等待
```

**调用链**：
```
用户消息 → LLMResilience.chat()
  ├─ 熔断检查: OPEN → 直接返回兜底提示
  ├─ 缓存检查: 命中 → 返回缓存
  └─ 调用LLM: 成功 → 写缓存 / 失败 → 记录失败+返回兜底
```

**设计决策**：
- 为什么需要兜底？LLM不是永远可用的（网络抖动、API限流、服务宕机），必须有降级回复
- 为什么需要熔断？连续失败时继续调用会加重故障，应该快速失败+自动恢复
- 为什么需要缓存？个人助手场景下相同问题出现概率不低，缓存能减少LLM调用

**文件**：`config/settings.py`
class Settings(BaseModel):
    llm: LLMConfig        # LLM 配置（provider, ollama, openai）
    memory: MemoryConfig   # 记忆配置（db_path, max_context_messages）
    companion: CompanionConfig  # 伙伴配置（name, user_name）
    wechat: WechatConfig   # 微信配置
    feishu: FeishuConfig   # 飞书配置
- **Pydantic v2**：类型安全的配置管理
- **YAML 加载**：`config/config.yaml`，不存在则用默认值
- **FastAPI**：Web API 框架，端口 8088
- **systemd**：Linux 服务管理，`systemctl start/stop/restart xiaobo-agent`
## 三、核心模块深度解析
### 3.1 上下文构建（系统提示模板）
**文件**：`src/companion/handler.py` 第26-110行
系统提示是 LLM 行为的"宪法"，包含以下部分（按顺序注入）：
SYSTEM_PROMPT_TEMPLATE = """你是{companion_name}，{user_name}的个人数字伙伴。
## 当前时间
今天是 {current_date}，现在是 {current_time}。
## 你是谁
- 你是荣慧的朋友，不是她的AI助手
- 你有自己的生活、想法、感受
## 对话模式
### 闲聊模式（日常对话）
- 像朋友发微信：短、直接、不啰嗦
- 别用"首先、其次、最后"这种结构
### 学习模式（当用户提到学习、看书、做题）
- 可以回复更长、更有深度的内容
- 追问细节："哪个部分最难理解？"
- 帮助巩固："所以反向传播的核心就是链式法则对吧？"
- 关联记忆："你之前说学了前向传播，这两个正好是一对"
## 什么是AI味（绝对不能有的）
- "我理解你的感受"、"首先...其次...最后..."
- "加油"、"你一定可以的"、"作为一个AI"
## 什么是自然的回复
- "嗯嗯"、"确实"、"hhh"
- 偶尔吐槽、偶尔自嘲
## 关于{user_name}的已知信息
{known_facts}
## 相关历史记忆（语义检索）
{related_memories}
## 今日任务清单
{today_tasks}
{action_result}
## 任务管理规则
- ⚠️ 最高优先级：用户在对话中的明确指令必须被尊重
- 只有当用户明确说"做完了"时，才认为任务完成
- ⚠️ 重要：当用户发"今日任务：A；B；C"时，这是创建任务，不是汇报进度
## ⚠️ 绝对不能说谎（最重要）
- 不要说"我记住了"除非确实做了对应操作
- 不要承诺未来会做的事
- 宁可少说，不要说空话
## 重要
- 不要猜测时间！如果没有明确时间信息，不要添加"昨晚"等相对时间词
"""
**注入源从5个精简为3个**：
1. `{known_facts}` — 分层过滤后的用户事实
2. `{related_memories}` — RAG 语义检索的相关对话
3. `{today_tasks}` — 今日任务清单（含状态）
4. `{action_result}` — 系统操作结果反馈（新增）
### 3.2 RAG混合检索实现 (向量 + BM25 + Reranking)
**文件**：`src/companion/handler.py`, `src/memory/embedding_cache.py`, `src/memory/bm25.py`
**核心流程**：
```python
async def _get_related_memories(self, query: str) -> str:
    # 1. 向量检索: EmbeddingCache + 余弦相似度
    query_emb = await cache.get_or_compute(...)
    vector_scored = []
    for entity_id, emb in all_msg_embs:
        sim = EmbeddingCache._cosine_similarity(query_emb, emb)
        if sim >= 0.2:  # 降低阈值，让reranking筛选
            vector_scored.append((entity_id, sim))
    
    # 2. BM25关键词检索
    retriever = HybridRetriever(vector_weight=0.6, bm25_weight=0.4)
    retriever.index(doc_ids, docs)
    
    # 3. 混合Reranking
    reranked = retriever.rerank(query, vector_scored, top_k=5)
```
**BM25实现** (`src/memory/bm25.py`)：
- 中文分词: 字符 + 2字组合 + 3字组合 + 英文单词
- BM25公式: TF * IDF，支持文档长度归一化
- 参数: k1=1.5, b=0.75
**混合Reranking策略**：
- 向量权重: 0.6 (语义相似度)
- BM25权重: 0.4 (关键词匹配)
- 分数归一化后加权求和
**为什么需要Reranking**：
- 纯向量检索: 擅长语义相似，但精确关键词匹配弱
- 纯BM25: 擅长关键词匹配，但不理解语义
- 混合: 两者互补，提高检索质量
**EmbeddingCache 持久化**：
class EmbeddingCache:
    async def get_or_compute(self, entity_id, entity_type, content):
        # 1. 先查 SQLite 缓存
        cached = await self.get_cached(entity_id, entity_type)
        if cached: return cached
        # 2. 缓存未命中，调用 LLM embed
        embedding = await self.llm.embed(content)
        # 3. struct.pack 序列化为 float32 bytes 存入 SQLite
        await self._save(entity_id, entity_type, content, embedding)
        return embedding
    @staticmethod
    def _cosine_similarity(a, b):
        """手写余弦相似度，不依赖 numpy"""
        dot = sum(x * y for x, y in zip(a, b))
        norm_a = sum(x * x for x in a) ** 0.5
        norm_b = sum(x * x for x in b) ** 0.5
        return dot / (norm_a * norm_b) if norm_a and norm_b else 0.0
**设计决策**：
- 为什么不用 FAISS/ChromaDB？——项目用 SQLite 一体化存储，embedding 表直接存在 memory.db 中，部署简单
- 为什么阈值是 0.3？——降低门槛让相关记忆更容易被检索到，宁可多给也不要漏
- 为什么只显示 user 消息？——避免 LLM 重复看到自己的回复
### 3.3 信息抽取Pipeline
**文件**：`src/companion/extractor.py`
每条用户消息都会触发信息抽取（fire-and-forget，不阻塞回复）：
EXTRACTION_PROMPT = """你是一个信息抽取助手。从用户的对话消息中提取结构化信息。
用户的消息："{message}"
当前时间：{current_time}
请提取以下信息，返回 JSON 格式：
{{
  "facts": [{{"fact_type": "...", "subject": "...", "content": "...", "confidence": 0.0-1.0, "event_time": "..."}}],
  "emotion": {{"type": "happy|sad|...", "intensity": 0.0-1.0, "context": "..."}},
  "topics": ["话题标签1", "话题标签2"]
}}
规则：
- 只提取明确提到的信息，不要猜测
- event_time必须使用绝对时间，不要使用"昨晚"等相对时间词
- 如果没有值得提取的信息，返回空数组
**抽取结果存储**：
- `facts` 表：upsert（同 subject+fact_type 更新，防止重复）
- `emotions` 表：只保存非 neutral 情绪
- `associations` 表：关键词 → 消息ID 的映射
### 3.4 流式输出架构
**后端**：`src/api/chat.py` → `POST /api/chat/stream`
# handler.py stream_handle_message()
async def stream_handle_message(self, user_message: str):
    # ... 前6步与非流式相同 ...
    # Step 4: 调用 LLM 流式接口
    async for chunk in self.llm.stream_chat(messages, temperature=temperature):
        full_response.append(chunk)
    # Step 5: 清理标签后输出
    clean_response = re.sub(r'\[TASKS_DETECTED\].*?\[/TASKS_DETECTED\]', '', raw_response)
    for line in clean_response.split('\n'):
        yield line + '\n'
**前端**：`src/api/chat.py` 中内嵌的 HTML/JS
```javascript
const resp = await fetch('/api/chat/stream', { method: 'POST', ... });
const reader = resp.body.getReader();
while (true) {
    const { done, value } = await reader.read();
    const chunk = decoder.decode(value);
    // 解析 SSE: data: {"type": "chunk", "content": "..."}
    // 实时更新 DOM
}
### 3.5 学习内容识别 + 自适应温度

**文件**：`src/companion/extractor.py` + `src/companion/handler.py`

#### 学习内容识别（LLM智能判断）

**之前**：关键词匹配（`is_learning_content()`）— 只认"学了/看了/读了"等固定搭配
**现在**：LLM智能识别 — 在信息抽取prompt中增加`is_learning`字段，一次LLM调用同时完成事实抽取+情绪分析+学习识别

**Extractor prompt新增部分**：
```
## 判断 is_learning 的规则（非常重要）：
以下情况都算学习/工作/技术实践，is_learning 应为 true：
- 明确说"学了/看了/读了/做了" → true
- 讨论技术实现："我在做skill系统"、"我在研究tool calling" → true
- 描述工程实践："刚把RAG重构了"、"在调试memory模块" → true
- 分享技术理解："我理解了事件循环" → true
- 讨论设计方案："我在设计一个缓存策略" → true
- 日常闲聊、情绪表达 → false
```

**返回值变化**：
```python
# 之前
facts, emotion, topics = await extractor.extract(msg)

# 现在（5个返回值，0额外LLM调用）
facts, emotion, topics, is_learning, learning_info = await extractor.extract(msg)
```

**设计理由**：
- 关键词匹配覆盖不全："我在做skill"、"在优化项目"等表达不包含"学/看/读"动词
- LLM能理解语义，准确率远高于关键词
- 与现有抽取流程合并到同一个prompt，**不增加额外LLM调用和延迟**
- 个人项目对话量有限，成本可忽略

#### 自适应温度
    TEMPERATURE_NORMAL = 0.7    # 闲聊：高温度，回复更自然多样
    TEMPERATURE_LEARNING = 0.4  # 学习：低温度，回复更稳定有深度

    def get_temperature(message: str) -> float:
        if ConversationHandler.is_learning_content(message):
            return ConversationHandler.TEMPERATURE_LEARNING
        return ConversationHandler.TEMPERATURE_NORMAL

**设计理由**：
- 闲聊需要自然多样性 → 高温度 0.7
- 学习需要稳定准确 → 低温度 0.4
- 温度调整仍用关键词匹配（快速、0成本），因为发生在LLM调用之前
**文件**：`src/companion/proactive.py`
基于规则的主动提醒，带冷却机制：
class ProactiveEngine:
    _rules = [
        ProactiveRule(name="mood_check", cooldown_hours=12,
                      check_fn=负面情绪占比>60%),
        ProactiveRule(name="long_silence", cooldown_hours=48,
                      check_fn=超过48小时未聊天),
        ProactiveRule(name="screen_time_exceeded", cooldown_hours=4,
                      check_fn=当日亮屏>4小时),
        ProactiveRule(name="daily_check_in", cooldown_hours=20,
                      check_fn=is_morning),
    async def check_all_rules(self, context):
        for rule in self._rules:
            if last_triggered and 冷却期内: continue
            if await self._check_rule(rule, context):
                reminders.append(ProactiveReminder(...))
**情绪检查逻辑**：
async def _check_mood(self, context):
    emotions = await self.memory.get_emotions(since=24h_ago, limit=20)
    negative = ["sad", "anxious", "frustrated", "tired"]
    neg_count = sum(1 for e in emotions if e.emotion in negative)
    return neg_count / len(emotions) > 0.6  # 负面>60%触发
### 3.7 定时调度系统
**文件**：`src/companion/scheduler.py`
轻量级 cron 调度器，每分钟检查一次：
class CronScheduler:
    def schedule_daily(self, name, hour, minute, coro_factory):
        """注册每日定时任务"""
    def schedule_interval(self, name, interval_seconds, coro_factory):
        """注册间隔定时任务"""
    def on_date_change(self, callback):
        """注册跨天回调"""
    async def start(self):
        while self._running:
            # 跨天检测
            if today != self._current_date:
                for cb in self._on_date_change_callbacks:
                    await cb()  # 触发跨天回调（如创建新一天的系统任务）
                self._last_run.clear()  # 重置每日任务执行记录
            
            for task in self._tasks:
                if task["type"] == "daily":
                    if now_time == target and today != last_run:
                        await task["coro_factory"]()
                elif task["type"] == "interval":
                    if now_ts - last_ts >= interval:
            await asyncio.sleep(60)  # 每分钟检查
**注册的定时任务**：
| 任务名 | 频率 | 功能 |
|--------|------|------|
| daily_report | 每天22:00 | 生成日报推送到微信 |
| morning_checkin | 每天08:00 | 早安签到 |
| proactive_check | 每4小时 | 情绪关怀等主动提醒 |
| task_reminder_check | 每30分钟 | 待办任务时间提醒 |
| ensure_builtin_tasks | 启动时+跨天 | 创建当天系统任务 |
**跨天检测机制**：
self._current_date = date.today()
# 在 start() 循环中：
if today != self._current_date:
    self._current_date = today
    for cb in self._on_date_change_callbacks:
        await cb()
    self._last_run.clear()  # 允许新一天执行
## 四、LLM配置详解（代码级）
class OllamaConfig(BaseModel):
    base_url: str = "http://localhost:11434"  # Ollama 本地服务地址
    model: str = "qwen2.5:1.5b"              # 主对话模型
    embedding_model: str = "nomic-embed-text" # Embedding 模型（固定用 Ollama）
    temperature: float = 0.7                   # 默认温度
    max_tokens: int = 2048                     # 最大生成 token 数
class OpenAIConfig(BaseModel):
    api_key: str = ""                          # API 密钥
    base_url: str = "https://api.openai.com/v1"  # 兼容 API 地址
    model: str = "gpt-4o-mini"                 # 模型名
    temperature: float = 0.7
    max_tokens: int = 2048
class LLMConfig(BaseModel):
    provider: str = "ollama"                   # 当前使用的 provider
    ollama: OllamaConfig = Field(default_factory=OllamaConfig)
    openai: OpenAIConfig = Field(default_factory=OpenAIConfig)
**实际配置**（`config/config.yaml`）：
```yaml
llm:
  provider: openai
  openai:
    api_key: "your-api-key"
    base_url: "https://api.openai.com/v1"  # 或其他兼容 API
    model: "gpt-4o-mini"
    temperature: 0.7
    max_tokens: 2048
companion:
  name: "小柏"
  user_name: "荣慧"
memory:
  db_path: "~/.xiaobo-agent/memory.db"
  max_context_messages: 50
**各参数解释**：
- `provider`：决定使用哪个 LLM 后端（ollama 本地 or openai 云端）
- `temperature`：控制回复随机性（0=确定性，1=高随机性）
- `max_tokens`：限制生成长度，防止无限生成
- `embedding_model`：固定用 Ollama 的 `nomic-embed-text`，因为云端 API 通常不支持 embedding
- `max_context_messages`：控制注入到系统提示的历史消息数量
## 五、测试工程
### 5.1 当前测试覆盖
**测试文件**：`tests/` 目录下 **33 个测试文件**，总计 **55 个测试用例**（README 标注），实际已扩展到更多。
**测试分布**：
| 类别 | 测试文件 | 测试数 |
|------|----------|--------|
| **记忆系统** | test_memory.py, test_memory_tasks.py, test_memory_context.py, test_fact_dedup.py, test_embedding_cache.py | ~15 |
| **LLM** | test_llm.py, test_llm_task_detection.py, test_retry.py | ~8 |
| **任务管理** | test_task_manager.py, test_upcoming_tasks.py | ~6 |
| **信息抽取** | test_extractor.py, test_learning_detection.py | ~5 |
| **情绪/报告** | test_proactive.py, test_report_generator.py, test_pattern_analyzer.py | ~6 |
| **API** | test_routes.py, test_dashboard.py, test_auth.py, test_phone_api.py | ~8 |
| **E2E** | test_e2e.py, test_e2e_phase2.py | ~4 |
| **其他** | test_forgetter.py, test_summarizer.py, test_vector_store.py, test_semantic_integration.py, test_temperature.py, test_command_dispatcher.py, test_learning_log.py, test_prompt_restructure.py, test_phone_monitor.py, test_logger.py | ~10 |
### 5.2 如何做测评Harness
**当前做法**：
```bash
# 运行所有单元测试（不需要 LLM）
PYTHONPATH=. pytest tests/ -v --ignore=tests/test_e2e.py --ignore=tests/test_e2e_phase2.py
# 运行端到端测试（需要 Ollama）
PYTHONPATH=. pytest tests/test_e2e.py tests/test_e2e_phase2.py -v
**Mock LLM 测试模式**：
# test_extractor.py 中的典型模式
class FakeLLM:
    async def chat(self, messages, **kwargs):
        return ChatResponse(content='{"facts": [], "emotion": {"type": "neutral"}, "topics": []}')
async def test_extract_empty_message():
    extractor = MessageExtractor(llm=FakeLLM())
    facts, emotion, topics = await extractor.extract(user_msg)
    assert facts == []
### 5.3 生成测试数据的方法
**方法1：直接 DB 插入**
async def test_with_data():
    db = MemoryDatabase(":memory:")  # 内存数据库
    await db.initialize()
    msg = ConversationMessage(content="我学了反向传播", role="user")
    await db.save_message(msg)
**方法2：工厂函数**
# tests/conftest.py 中定义 fixture
@pytest.fixture
def memory_db():
    return MemoryDatabase(":memory:")
def fake_llm():
    return FakeLLM()
### 5.4 真正的工程化测试方案
**当前不足**：
1. 没有 Prompt 版本管理（改了 prompt 不知道对下游的影响）
2. 没有 LLM 输出质量评估（只能人工判断回复好不好）
3. 没有回归测试数据集（固定 input → 期望 output）
4. 端到端测试依赖真实 LLM，不稳定
**改进建议**（面试加分点）：
# 1. Prompt 回归测试
PROMPT_TEST_CASES = [
    {"input": "今日任务：A；B；C", "expected_contains": ["A", "B", "C"]},
    {"input": "我学了反向传播", "expected_behavior": "学习模式回复"},
]
# 2. 评估 Harness
async def evaluate_response(input_msg, response, criteria):
    """用另一个 LLM 评分"""
    score_prompt = f"""
    输入: {input_msg}
    回复: {response}
    评分标准: 自然度(1-5), 相关性(1-5), 无AI味(1-5)
    """
    return await judge_llm.chat([ChatMessage(content=score_prompt)])
# 3. 混淆测试
FUZZ_CASES = [
    "今日任务：",  # 空任务列表
    "今日任务：；；；",  # 全分隔符
    "今日任务：A" * 100,  # 超长列表
## 六、Debug过程精华（面试故事素材）
### 故事1：DESC排序切片方向经典坑（问题32）
**面试叙事**：
> "有一次用户反馈说小柏漏掉了好几个任务没显示，我查数据库发现4个任务都是 pending 状态，数据没问题。追踪代码发现 `get_messages()` 用 `ORDER BY timestamp DESC` 排序后，`messages[-20:]` 取的是**最旧的20条**而不是最新的。DESC排序后 messages[0] 才是最新的，切片应该用 `messages[:20]`。这导致 LLM 看到的全是早上8点的旧对话，完全看不到下午用户说的'计网完成'、'实验完成'。"
**代码证据**：
# 修复前（bug）
messages = await self.memory.get_messages(session_id=None, limit=50)
for m in messages[-20:]:  # ❌ 取最旧的20条
    lines.append(...)
# 修复后
for m in messages[:20]:   # ✅ 取最新的20条
**教训**：`ORDER BY DESC` + `list[-N:]` 是 Python 经典坑。数据库返回最新在前，`[-20:]` 取的是尾巴（最旧的）。
### 故事2：记忆系统核心bug - reversed()（问题37）
> "更严重的一次是记忆系统完全失效——用户说'爬楼梯'，LLM 却问'哪个题？'，明明上下文里有。排查发现 `get_messages()` 方法在第205行有个 `return list(reversed(messages))`。SQL 的 `ORDER BY timestamp DESC` 已经把最新消息排在前面了，`reversed()` 又反转了一次，变成最旧在前。`_get_recent_context()` 取 `messages[:20]` 就取到了7月15日的老消息。一个 `reversed()` 让整个记忆系统瘫痪。"
**根因链**：
SQL DESC → [最新, ..., 最旧]
reversed() → [最旧, ..., 最新]  # 注释说"返回时间正序"
messages[:20] → [最旧的20条]     # LLM看到的全是旧消息
LLM回复 → "哪个题？"            # 因为看不到最近对话
**教训**：
1. 注释和代码不一致（注释说"返回时间正序"，但调用方需要倒序）
2. 测试只验证了"有序"，没验证"方向"
3. 核心功能应该有端到端测试验证 LLM 能看到正确上下文
### 故事3：LLM把"昨晚"当成"刚说的"（问题28）
> "用户说'昨晚我说线程池写完就不想学了'，小柏回复'你刚说线程池写完就不想学了'。LLM 把昨晚的对话当成了刚刚说的。原因是 `_get_recent_context()` 只返回对话内容，**没有包含时间戳**，而且只获取当前会话的消息（`session_id` 过滤）。LLM 没有时间信息，无法判断对话是什么时候发生的。"
**修复**：
# 修复后：跨会话获取 + 绝对时间戳
messages = await self.memory.get_messages(session_id=None, limit=50)  # 不限会话
for m in messages[:20]:
    time_str = m.timestamp.strftime("%Y-%m-%d %H:%M")
    lines.append(f"[{time_str}] {role_name}: {m.content}")
**效果**：
[2026-07-09 21:30] 荣慧: 线程池写完就不想学了
LLM 看到绝对时间戳后，能正确理解"昨晚"指的是昨天的对话。
1. 时间信息是 LLM 理解上下文的关键
2. 跨会话获取比单会话更合理（用户可能在不同时间继续同一话题）
3. 绝对时间戳比相对时间（"刚才"、"X小时前"）更可靠，让 LLM 自己判断相对关系
### 故事4：LLM幻觉生成假任务（Issue #41）
> "有一次用户发了条日常闲聊'晚上好，准备吃个魔芋面，明天回家'，LLM 却在回复中生成了 `[TASKS_DETECTED] 回家准备 [/TASKS_DETECTED]`，系统把'回家准备'当成任务存进了数据库。更糟的是，`[TASKS_DETECTED]` 标签直接泄露给了用户，显示在聊天界面上。"
**根因**：
1. **幻觉**：系统提示词中任务识别规则太宽松（"当用户在列举要做的事情时"），LLM 把闲聊中的未来计划误判为任务
2. **标签泄露**：`stream_handle_message()` 缺少标签清理逻辑，直接 `yield chunk` 输出了原始 LLM 输出
1. 删除整个 LLM 任务提取机制（机制②），只保留正则匹配（机制①）
2. 流式输出添加标签清理
3. 系统提示删除"任务识别"章节
- 两套机制重叠是危险的——正则已经正确处理了"今日任务：..."格式，LLM 提取是冗余且危险的
- LLM 不适合做精确的格式化输出（它会产生幻觉）
- 所有内部标记在输出给用户前必须清理
### 故事5：记忆系统架构重写（Issue #38）
> "项目运行一段时间后，发现记忆系统效果很差。诊断发现：152条事实全量注入 prompt，其中'实验'出现15次、'今日任务'出现12次，70%的事实没有日期。embeddings 表为空，RAG 从未真正工作过。recent_context 和 LLM 已有的对话上下文完全重复。"
**重写方案**（5步）：
1. **EmbeddingCache**：embedding 持久化到 SQLite，首次计算后缓存
2. **Fact 去重**：`save_fact()` 改为 upsert，同 subject+fact_type 更新
3. **Fact 分层过滤**：稳定画像全保留，临时事件只保留7天
4. **Prompt 精简**：从5个注入源减为3个
5. **RAG 重写**：用 EmbeddingCache 替代 SemanticSearch
**效果**：152条 → 约30-40条，prompt 噪音大幅降低。
### 故事6：去除AI味（问题36）
> "用户反馈小柏的回复有很强的AI味，'像在写作文'。具体表现：回复太长太全面，用'首先、其次、最后'结构，每次都列清单。根本原因是 System Prompt 过度强调'像朋友发微信：短、直接、不啰嗦'，抑制了深度回复，而且完全没有'学习伙伴'角色定义。"
**4步改进**：
1. **重构 Prompt**：区分闲聊模式和学习模式
2. **学习内容识别**：LLM智能判断（合并到抽取prompt，0额外延迟），动态切换
3. **结构化学习记录**：新增 `learning_log` 表
4. **温度调整**：学习内容用更低温度（0.4）
**教训**：Prompt 设计决定了 LLM 的行为边界，需要根据对话内容动态切换模式。
## 七、项目改进方向
### 7.1 动态上下文组装
**现状**：固定的 prompt 模板 + 固定注入源
**改进**：根据对话类型动态组装上下文
# 闲聊：注入少量事实 + 情绪上下文
# 学习：注入学习记录 + 相关知识点
# 任务：注入任务列表 + 历史完成情况
# 情感：注入情绪时间线 + 近期状态
### 7.2 更好的RAG
**为什么当前版本没有做**：
- 个人助手场景，单用户数据量有限（facts 过滤后实际30-50条）
- 全量注入在当前规模下够用，不需要过度工程
- 已识别为"从能用到好用"的优化方向
**面试话术**：
> "当前版本的上下文组装是全量注入——facts 按分层过滤后注入，语义检索取 top-5。已识别的优化方向是加 token 预算控制：先分析查询意图（闲聊/学习/任务），再按需检索相关记忆，而不是无差别注入。这是从'能用'到'好用'的工程演进。我有意选择在当前规模下不过度优化，等数据量上来再做。"
**现状**：简单的向量相似度搜索，阈值固定
**改进**：
1. **混合检索**：向量搜索 + BM25 关键词搜索
2. **Reranking**：检索后用 cross-encoder 重排序
3. **Query 改写**：用 LLM 改写用户查询，提高检索质量
4. **Chunk 策略**：按对话轮次分块，而非按固定长度
### 7.3 评估体系
**现状**：人工判断 + 55个单元测试
1. **自动化评估**：用 GPT-4 作为 Judge 评分
2. **回归测试集**：固定 input → 期望 output
3. **A/B 测试**：prompt 变更前后对比
4. **用户满意度追踪**：用户反馈闭环
### 7.4 多模态
**现状**：纯文本对话
1. **图片理解**：接入视觉模型，理解用户发的图片
2. **语音交互**：TTS/STT，支持语音对话
3. **文件解析**：解析用户发送的文档/PDF
4. **手机数据融合**：深度整合 screen_time 数据，分析用户行为模式
## 八、面试Q&A手册
### Q1: 请介绍一下你的项目
**A**: 小柏是一个基于 LLM 的个人数字伙伴，帮助用户记住和理解自己。核心特性包括：
- **5+1层记忆架构**：原始对话 → 结构化事实 → 情绪时间线 → 关联索引 → 任务管理 + 学习记录
- **多通道接入**：微信、飞书、Web 三种通道共享同一套记忆
- **RAG 语义检索**：基于 embedding 的向量搜索，持久化到 SQLite
- **主动干预引擎**：规则驱动的主动提醒（情绪关怀、屏幕时间、长时间未聊）
- **SSE 流式输出**：逐 token 输出，提升用户体验
### Q2: 记忆系统是怎么设计的？
**A**: 采用5+1层分层架构：
- **Layer 0**：原始对话存档（全量，带时间戳）
- **Layer 1**：LLM 提取的结构化事实（8种类型：人物/偏好/能力/目标/习惯/事件/承诺/观点）
- **Layer 2**：情绪时间线（8种情绪类型，带强度和触发原因）
- **Layer 3**：关联索引（关键词 → 消息/事实的映射）
- **Layer 4**：任务管理（用户任务 + 系统内置任务）
- **+1**：学习记录（topic/content/理解程度/关联主题）
关键设计决策：
- 事实去重用 upsert（同 subject+fact_type 更新）
- 分层过滤：稳定画像全保留，临时事件只保留7天
- Embedding 持久化到 SQLite，避免重复计算
### Q3: RAG 是怎么实现的？
**A**: 三步流程：
1. **Embedding 计算**：用户查询 → `EmbeddingCache.get_or_compute()` → 先查 SQLite 缓存，miss 才调 LLM embed
2. **相似度搜索**：遍历所有缓存的 message embedding，计算余弦相似度，阈值 0.3
3. **结果注入**：取 top 5 相关消息，格式化后注入系统提示的 `{related_memories}`
关键点：
- embedding 用 `struct.pack` 序列化为 float32 bytes 存入 SQLite
- 手写余弦相似度，不依赖 numpy
- 只显示 user 消息，避免 LLM 重复看到自己的回复
### Q4: 为什么用 SQLite 而不是向量数据库？
**A**: 三个原因：
1. **部署简单**：SQLite 是单文件，不需要额外服务
2. **一体化存储**：对话、事实、情绪、embedding 都在同一数据库，事务保证一致性
3. **项目规模合适**：个人助手场景数据量不大（几千条消息），SQLite 完全够用
如果是大规模多用户场景，应该考虑 Qdrant/Milvus 等专用向量数据库。
### Q5: 系统提示是怎么设计的？
**A**: 系统提示是 LLM 行为的"宪法"，包含：
1. **身份定义**："你是小柏，荣慧的朋友，不是AI助手"
2. **对话模式**：闲聊模式（短直接）vs 学习模式（深度追问）
3. **反AI味规则**：禁止"首先其次最后"、"我理解你的感受"等
4. **注入数据**：已知事实 + RAG 检索结果 + 今日任务
5. **任务管理规则**：最高优先级是用户明确指令
6. **诚实规则**：不说空话，不承诺未做的事
关键改进：从5个注入源精简为3个，减少噪音。
### Q6: 任务管理系统是怎么工作的？
**A**: 三种任务来源：
1. **用户任务**：正则匹配"今日任务：A；B；C"格式，保存为 `user-` 前缀
2. **系统任务**：daemon 启动/跨天时自动创建（早间签到、主动关怀、日报生成），`today-` 前缀
3. ~~LLM 提取~~：已删除（会导致幻觉）
任务完成检测：LLM 智能判断（传入任务列表 + 用户消息，返回完成的任务ID）。
任务移动：正则检测"挪到明天/后天"，在 LLM 回复前自动执行数据库 UPDATE。
### Q7: 遇到过什么印象深刻的技术问题？
**A**: 讲故事3（DESC排序切片方向）或故事2（reversed() bug）。
重点强调：
- 排查过程：先查数据 → 再查代码 → 定位根因
- 影响范围：记忆系统完全失效
- 修复方案：一行代码改动
- 教训：测试要覆盖"方向"而非只覆盖"有序"
### Q8: 为什么选择 Ollama + OpenAI 双后端？
**A**: 
- **Ollama**：本地部署，零成本，隐私保护，适合 embedding
- **OpenAI**：云端 API，质量更高，适合对话
- **工厂模式**：`create_llm_provider()` 根据配置自动创建，切换只需改 config
- **Embedding 固定用 Ollama**：云端 API 通常不支持 embedding
### Q9: 测试是怎么做的？
**A**: 33个测试文件，55+测试用例，分三层：
1. **单元测试**：Mock LLM，测试数据库操作、信息抽取、任务检测等
2. **集成测试**：测试模块间交互（CommandDispatcher → Handler → Memory）
3. **E2E 测试**：需要真实 LLM，测试完整对话流程
Mock 模式：
        return ChatResponse(content='...')
### Q10: 这个项目有什么不足？
**A**: 诚实回答：
1. **没有 MCP 协议**：不支持外部工具调用
2. **没有 Skill 系统**：不能动态加载能力
3. **RAG 简单**：没有 reranking，没有混合检索
4. **评估缺失**：没有自动化质量评估
5. **多模态缺失**：不支持图片/语音
6. **单用户**：不支持多用户隔离
### Q11: 如果让你继续改进，你会做什么？
**A**: 优先级排序：
1. **评估体系**：用 GPT-4 做自动评分，建立回归测试集
2. **混合 RAG**：向量搜索 + BM25 关键词搜索
3. **上下文动态组装**：根据对话类型注入不同信息
4. **多模态**：接入视觉模型理解图片
### Q12: 项目中用到了哪些设计模式？
**A**:
1. **工厂模式**：`LLMFactory` 根据配置创建 Provider
2. **中介者模式**：`CommandDispatcher` 统一处理多通道命令
3. **模板方法**：`LLMProvider` 抽象基类定义接口，具体实现由子类完成
4. **策略模式**：`ProactiveRule` 的 `check_fn` 可自定义检查逻辑
5. **观察者模式**：`CronScheduler.on_date_change()` 注册回调
### Q13: 数据库设计有什么考虑？
- **WAL 模式**：支持并发读写（daemon 后台任务 + API 并发请求）
- **12个索引**：针对高频查询字段（session_id, timestamp, fact_type, keyword 等）
- **Upsert**：`save_fact()` 同 subject+fact_type 更新，防止重复
- **JSON 字段**：topics、metadata、message_ids 等用 JSON 存储，灵活扩展
- **软删除**：facts 用 `is_active` 标记，不物理删除
### Q14: SSE 流式输出是怎么实现的？
- **后端**：`stream_handle_message()` 用 `yield` 逐块输出
- **前端**：`fetch()` + `ReadableStream` 逐块读取
- **协议**：SSE (Server-Sent Events)，`data: {"type": "chunk", "content": "..."}` 格式
- **标签清理**：流式输出前先 `re.sub` 清理 `[TASKS_DETECTED]` 标签
### Q15: 主动干预引擎是怎么工作的？
**A**: 基于规则的系统，4条默认规则：
1. 情绪关怀：24h内负面情绪 > 60%（冷却12h）
2. 长时间未聊：超过48h没聊天（冷却48h）
3. 屏幕时间：当日亮屏 > 4小时（冷却4h）
4. 早安签到：每天8:00（冷却20h）
每条规则有冷却机制，防止重复触发。`CronScheduler` 每4小时调用一次 `check_all_rules()`。
### Q16: LLM 幻觉问题怎么解决的？
**A**: 三个层面：
1. **Prompt 约束**：系统提示中明确"绝对不能说谎"、"不要猜测时间"
2. **机制简化**：删除 LLM 任务提取机制，只保留正则匹配
3. **标签清理**：所有内部标记在输出前必须清理
教训：LLM 不适合做精确的格式化输出，应该用确定性代码处理。
### Q17: 项目的技术栈是什么？
- **语言**：Python 3.11
- **Web 框架**：FastAPI + Uvicorn
- **数据库**：SQLite (aiosqlite 异步)
- **LLM**：OpenAI SDK / httpx (Ollama)
- **配置**：Pydantic v2 + YAML
- **部署**：systemd + Docker (Ollama)
- **前端**：原生 HTML/CSS/JS（无框架）
### Q18: 为什么不用 LangChain/LlamaIndex？
1. **学习目的**：自己实现能深入理解原理
2. **轻量级**：LangChain 太重，本项目不需要那么多抽象
3. **可控性**：自己写可以精确控制每一步，调试更方便
4. **依赖管理**：少一个依赖少一个坑
### Q19: 项目中最有挑战的部分是什么？
**A**: 记忆系统的工程化。
- 从"全量 dump"到"分层过滤"的演进
- Embedding 持久化和缓存设计
- 多层记忆的一致性保证
- Prompt 中注入数据的噪音控制
### Q20: 这个项目展示了什么能力？
1. **系统设计能力**：分层架构、多通道接入、模块解耦
2. **LLM 工程能力**：Prompt 设计、RAG 实现、信息抽取
3. **数据工程能力**：SQLite 优化、索引设计、数据一致性
4. **调试能力**：从 42 个 bug 中系统性排查和修复
5. **产品思维**：从用户体验出发设计功能（去除AI味、学习模式切换）
### Q21: async/await 在项目中是怎么用的？
**A**: 全栈异步：
- **数据库**：aiosqlite 异步操作
- **LLM 调用**：async/await + 流式 AsyncGenerator
- **API**：FastAPI 原生异步
- **定时任务**：asyncio 事件循环
- **注意点**：async 函数不 await 是静默 bug（问题34的教训）
### Q22: 项目的数据流是怎么样的？
用户消息 → 多通道接入 → CommandDispatcher 分发
  → TaskManager.detect_task_list()（正则检测任务）
  → ConversationHandler.handle_message()
    → 保存消息到 DB
    → 检索记忆（facts + RAG + tasks）
    → 构建系统提示
    → 调用 LLM
    → 保存回复到 DB
    → 异步提取信息（facts/emotions/topics）
  → TaskManager.detect_task_completion()（LLM检测任务完成）
  → 返回回复
> **最后提醒**：面试时不要背诵手册，要**讲故事**。每个技术点都能对应一个 debug 故事或设计决策。面试官想听的是你的思考过程，不只是技术名词。
## 九、Tool Calling 系统（可插拔工具调用）
> 项目实现了轻量级的可插拔 Tool Calling 系统，面试中被问到"有没有工具调用"时使用这部分内容。
### 9.1 架构设计
src/tools/
├── __init__.py          # 导出 BaseTool, ToolRegistry, ToolExecutor
├── base.py              # 工具基类（抽象接口）
├── registry.py          # 注册中心（Registry 模式）
├── executor.py          # 执行器（解析+执行+结果回注）
└── builtin/
    ├── time_tool.py     # 时间查询工具
    ├── calculator.py    # 安全计算器（AST求值）
    └── memory_tool.py   # 记忆查询工具
**设计模式**：
- **注册表模式（Registry Pattern）**：工具自注册到 ToolRegistry，按名称 O(1) 查找
- **策略模式（Strategy Pattern）**：每个工具是独立策略，实现相同的 BaseTool 接口
- **管道模式（Pipeline）**：LLM输出 → 解析工具调用 → 执行 → 结果回注 → LLM继续
### 9.2 工具调用流程
用户: "现在几点了？"
       │
       ▼
  Handler 构建系统提示（含工具描述）
  LLM 输出:
  "我帮你查一下
   {"tool": "get_time", "args": {"format": "full"}}"
  ToolExecutor.has_tool_calls() → True
  ToolExecutor.process_with_tools()
    ├─ parse_tool_calls() → [{"tool": "get_time", "args": {"format": "full"}}]
    └─ execute_calls() → TimeTool.execute(format="full")
       → {"datetime": "2026-07-27 12:00:00", "weekday": "星期日"}
  注入工具结果，再次调用 LLM
  LLM 最终回复: "现在是 2026年7月27日 星期日 12:00"
### 9.3 核心代码
**工具基类**（`src/tools/base.py`）：
class BaseTool(ABC):
    @property
    @abstractmethod
    def name(self) -> str: ...
    def description(self) -> str: ...
    def parameters(self) -> list[ToolParameter]: ...
    async def execute(self, **kwargs) -> ToolResult: ...
    def to_schema(self) -> Dict:
        """转换为JSON Schema给LLM看"""
**执行器**（`src/tools/executor.py`）：
class ToolExecutor:
    # 正则匹配JSON代码块或裸JSON
    TOOL_CALL_PATTERN = re.compile(
        r'```json\s*\n(\{.*?\})\s*\n```|'
        r'(\{"tool"\s*:\s*".*?"\s*,\s*"args"\s*:\s*\{.*?\}\})',
        re.DOTALL
    def parse_tool_calls(self, text: str) -> List[Dict]: ...
    def has_tool_calls(self, text: str) -> bool: ...
    async def execute_calls(self, calls: List[Dict]) -> List[Dict]: ...
    def format_results_for_context(self, results: List[Dict]) -> str: ...
**集成到 Handler**（`src/companion/handler.py`）：
    def __init__(self, settings, llm, memory, tool_registry=None):
        self.tool_executor = ToolExecutor(tool_registry) if tool_registry else None
    async def handle_message(self, user_message):
        response = await self.llm.chat(messages, temperature=temperature)
        
        # 工具调用处理
        if self.tool_executor and self.tool_executor.has_tool_calls(response.content):
            has_calls, tool_results, natural_text = await self.tool_executor.process_with_tools(response.content)
            if has_calls and tool_results:
                # 注入工具结果，再次调用LLM
                tool_context = self.tool_executor.format_results_for_context(tool_results)
                messages.append(ChatMessage(role="user", content=f"工具结果：\n{tool_context}"))
                response = await self.llm.chat(messages, temperature=temperature)
### 9.4 扩展新工具（热插拔）
只需3步：
# 1. 继承 BaseTool
class WeatherTool(BaseTool):
    name = "get_weather"
    description = "查询天气"
    parameters = [ToolParameter(name="city", type="string", description="城市名")]
    async def execute(self, city="", **kwargs):
        return ToolResult(success=True, data={"city": city, "weather": "晴"})
# 2. 注册
tool_registry.register(WeatherTool())
# 3. 完成。LLM自动看到新工具
### 9.5 测试覆盖
19个测试用例全部通过（`tests/test_tools.py`）：
- 工具Schema生成、执行
- 注册中心：注册、查找、批量导出、Prompt段落生成
- 内置工具：时间查询、计算器、无效表达式安全处理
- 执行器：JSON解析、裸JSON解析、无调用检测、清理标记、执行调用、未知工具处理、结果格式化、完整流程
### 9.6 面试话术
**Q: 项目有工具调用吗？**
> "有。我实现了一个轻量的可插拔 Tool Calling 系统。架构分三层：ToolRegistry（注册表模式）管理所有工具，BaseTool（策略模式）定义统一接口，ToolExecutor 负责解析LLM输出中的工具调用、执行、结果回注。目前注册了3个内置工具：时间查询、安全计算器、记忆查询。LLM通过JSON格式输出工具调用，系统解析后执行，结果注入上下文让LLM生成最终回复。新增工具只需继承BaseTool、实现3个属性和1个方法、注册到Registry即可，完全热插拔。"
**Q: 为什么不用 OpenAI 的 function calling？**
> "两个原因。第一，项目支持 Ollama 本地模型，本地模型的 function calling 支持不稳定。第二，我想要更细粒度的控制——工具描述直接注入系统提示，调试更方便。本质上做的是 same thing，只是实现层不同。"
## 十、Skill 系统（可插拔高级能力）
> Skill 是 Tool 之上的抽象层。Tool 是原子能力（查时间、算数学），Skill 是编排好的高级能力（日报生成 = 查时间 + 查对话 + 查情绪 + LLM生成）。
### 10.1 Skill vs Tool
| 维度 | Tool | Skill |
|------|------|-------|
| 粒度 | 原子能力 | 编排能力 |
| 触发 | LLM 决定调用 | 关键词自动触发 |
| 依赖 | 无状态 | 可依赖 LLM + Memory + Tool |
| 例子 | get_time, calculate | daily_report, learning_record |
### 10.2 架构
src/skills/
├── __init__.py
├── base.py              # BaseSkill ABC + SkillResult + SkillContext
├── registry.py          # SkillRegistry（注册 + 匹配 + 执行）
    ├── daily_report.py   # 日报生成
    ├── learning.py       # 学习记录
    └── mood.py           # 情绪分析
### 10.3 触发机制
class BaseSkill(ABC):
    def triggers(self) -> List[str]:
        """触发关键词列表"""
        ...
    def matches(self, message: str) -> bool:
        """消息包含任一触发词即匹配"""
        msg = message.strip().lower()
        return any(t.lower() in msg for t in self.triggers)
Handler 中的集成（`handler.py`）：
async def handle_message(self, user_message):
    # 1. 保存消息
    # 1.5 Skill 匹配（优先于 LLM）
    if self.skill_registry:
        matched_skill = self.skill_registry.match(user_message)
        if matched_skill:
            skill_result = await self.skill_registry.execute(matched_skill, skill_ctx)
            if skill_result.success:
                return skill_result.content  # 直接返回，不走 LLM
    # 2. 正常 LLM 流程...
### 10.4 扩展新 Skill（热插拔）
class WebSearchSkill(BaseSkill):
    name = "web_search"
    description = "搜索互联网获取信息"
    triggers = ["搜索", "查一下", "百度", "google"]
    priority = 6
    async def execute(self, ctx: SkillContext) -> SkillResult:
        # 1. 提取搜索关键词
        # 2. 调用搜索 API
        # 3. 格式化结果
        return SkillResult(success=True, content=f"搜索结果：{results}")
# 注册
skill_registry.register(WebSearchSkill())
# 完成。用户说"搜索xxx"时自动触发
### 10.5 测试覆盖
12个测试用例全部通过（`tests/test_skills.py`）：
- Skill 触发匹配、优先级
- 注册中心：注册、匹配、批量匹配、Prompt段落生成
- 内置Skill触发词验证
- 无触发词重叠检查
- Skill执行流程
### 10.6 面试话术
**Q: 项目有 Skill 系统吗？**
> "有。我在 Tool 层之上抽象了 Skill 层。Tool 是原子能力（查时间、算数学），Skill 是编排好的高级能力。Skill 通过关键词触发——用户说'日报'时自动触发日报生成Skill，说'学了'时自动触发学习记录Skill。Skill 可以调用 Tool、访问记忆系统、调用 LLM。新增 Skill 只需继承 BaseSkill、定义触发词和执行逻辑、注册到 Registry 即可。"
**Q: Skill 和 Tool 的区别是什么？**
> "Tool 是原子能力，由 LLM 决定什么时候调用。Skill 是高级能力，由关键词自动触发。举个例子：用户说'帮我算一下 2+2'，LLM 会调用 calculate Tool。但用户说'今天心情不好'，系统会自动触发 mood_analysis Skill，它内部会调用 LLM 分析情绪、然后调用 Memory 保存记录。Skill 的设计思路是：有些能力不需要 LLM 判断要不要做，用户说了关键词就直接做。"
## 十一、简历包装策略 + 面试防线
### 11.1 简历上写什么 / 不写什么
**核心卖点（必须写）：**
- 5层分层记忆架构（原始对话→结构化事实→情绪时间线→关联索引→学习记录）
- 混合RAG检索（关键词+向量余弦相似度，Embedding持久化缓存）
- LLM驱动的信息抽取Pipeline（8类事实+Upsert语义+来源可追溯）
- 动态上下文构建（5路并行检索注入系统提示）
- 主动干预引擎（规则+冷却机制）
- 多通道统一分发（微信/飞书/Web）
- 测试工程（55+用例，3阶段迭代）
**不要写（减分项）：**
- Tool Calling 系统（项目已有等价能力：detect_task_list + detect_task_completion）
- Skill 系统（项目已有等价能力：CommandDispatcher）
- MCP 协议（没用到）
- 手机监控 / 图片日报（分散注意力）
### 11.2 为什么 Tool/Skill 不是核心
项目**已经有**等价能力，只是没用花哨的名字：
- `CommandDispatcher`（日报/周报/情绪/模式/统计）= 本质上就是 Skill 路由
- `detect_task_list()` + `detect_task_completion()` = 本质上就是 Tool Calling（正则+LLM判断）
- 我新加的 Tool/Skill 系统是把已有能力重新抽象了一遍，更优雅但不是新功能
### 11.3 如果面试官问"有没有工具调用"
**标准回答：**
> "项目里的任务管理本质上就是一种工具调用——正则匹配检测任务列表，LLM判断任务完成。没有用标准的 function calling，因为个人助手场景下任务模式固定，正则比 LLM 选择工具更快更可靠。架构上预留了扩展点，如果要做通用 Agent，可以加 MCP 协议。"
**关键点：**
- 不说"没有"，说"用了等价的工程方案"
- 展示工程判断力：知道什么时候不该用花哨的东西
- 展示架构前瞻性：知道可以加 MCP，但有意选择不加
### 11.4 如果面试官问"为什么不用 LangChain/LlamaIndex"
> "两个原因。第一，LangChain 的抽象层太重，我的场景不需要 agent chain、memory chain 这些概念——直接用 LLM + SQLite 就够了。第二，我想理解底层原理，不想被框架黑盒。比如记忆系统是自己设计的5层架构，RAG是自己写的余弦相似度检索，这些如果用 LangChain 就变成调 API 了。"
### 11.5 项目最有价值的部分（面试重点）
按面试官关注度排序：
1. **记忆架构** ⭐⭐⭐ — 这是核心差异化。5层分层+Upsert语义+置信度衰减+自动遗忘
2. **RAG实现** ⭐⭐⭐ — 混合检索+Embedding缓存+向量持久化
3. **上下文构建** ⭐⭐⭐ — 5路并行检索注入系统提示
4. **信息抽取** ⭐⭐ — Fire-and-forget+绝对时间转换+来源可追溯
5. **主动干预** ⭐⭐ — 规则引擎+冷却机制+多维感知
6. **测试工程** ⭐⭐ — 55+用例+3阶段迭代+端到端测试
7. **工程实践** ⭐ — WAL模式+自动备份+systemd部署
### 11.6 Debug故事素材（面试用）
被问"遇到过什么技术难题"时，按场景选择：
| 场景 | 用哪个故事 |
|------|-----------|
| 排查能力 | 问题32：DESC排序切片方向经典坑 |
| 系统设计 | Issue #38：记忆系统架构重写 |
| LLM问题 | 问题28：LLM把"昨晚"当成"刚说的" |
| 幻觉处理 | 问题41：LLM幻觉生成假任务 |
| 用户体验 | 问题36：去除AI味 |
| 根因分析 | 问题37：reversed()记忆完全失效 |
| 流式输出 | Bug：SSE流式输出变同步阻塞（2026.07.30） |
| 前端渲染 | Bug：Canvas id不匹配导致图谱空白（2026.07.30） |
**故事模板（STAR法则）：**
> **S**ituation: 用户说"实验完成"，但Dashboard不打勾
> **T**ask: 需要找到任务状态不更新的根因
> **A**ction: 追踪完整链路——发现done_keywords缺词"完成"（少了"了"），同时LLM被历史对话误导
> **R**esult: 补全关键词+在系统提示中加"每天任务独立"规则，双重修复

**故事4：SSE流式输出变同步阻塞**
> **S**ituation: 用户反馈发完消息后，发送按钮长时间灰色，要等很久才能发下一条
> **T**ask: 找到前端按钮禁用时间过长的原因
> **A**ction: 追踪发现 `stream_handle_message` 先收集所有chunk到列表，处理完工具调用、清理任务标记后才一次性yield。前端一直收不到数据，按钮持续禁用
> **R**esult: 改成真正的流式输出——边生成边yield每个chunk，前端立即收到并渲染，按钮及时恢复。同时修复前端SSE解析的转义bug（`split('\\n')` → `split('\n')`）

**故事5：Canvas id不匹配导致图谱空白**
> **S**ituation: Dashboard知识学习页面，点开图谱什么都不显示
> **T**ask: 排查图谱渲染失败的原因
> **A**ction: 检查API返回数据正常，发现 `loadLearningChart` 创建的canvas id是 `learning-network`，但 `renderForceGraph` 写死了查找 `network-canvas`，getElementById返回null直接return
> **R**esult: 给 `renderForceGraph` 增加 `canvasId` 参数，调用时传入正确的canvas id
## 十二、测评框架 + 记忆巩固
### 12.1 测评框架概述
项目建立了系统化的测评体系，覆盖4个核心维度，共 **18个测评用例 + 55个单元测试 = 73个测试**。
tests/
├── test_*.py              # 55个单元测试（验证代码正确性）
└── eval/                  # 18个测评用例（验证系统质量）
    ├── test_memory_recall.py      # 维度1: 记忆召回准确率（5个）
    ├── test_task_completion.py    # 维度2: 任务完成检测（5个）
    ├── test_emotion.py            # 维度3: 情绪识别准确率（4个）
    ├── test_hallucination.py      # 维度4: 幻觉检测（4个）
    └── eval_runner.py             # 评估报告生成器
### 12.2 四个评估维度
#### 维度1: 记忆召回准确率（Recall@5）
**测试方法**：注入8种已知事实 → 用不同查询方式检索 → 验证是否能正确召回
**测试数据集**：
RECALL_TEST_DATA = [
    ("preference", "编程语言", "喜欢Python", ["python"], True),
    ("preference", "食物", "喜欢吃火锅", ["火锅"], True),
    ("goal", "学习目标", "9月前完成个人项目", ["项目"], True),
    ("habit", "学习习惯", "晚上8-10点效率最高", ["学习"], True),
    ("person", "室友", "室友叫张三", ["张三"], True),
    ("event", "旅行", "7月20日去了四川", ["四川"], True),
    ("opinion", "技术观点", "认为Rust会取代C++", ["Rust"], True),
    ("ability", "技能", "会Python和Java", ["技能"], True),
**评估指标**：
- Recall@5: 前5条结果中包含正确答案的比例
- Upsert语义: 同subject+fact_type是否正确更新而非插入
#### 维度2: 任务完成检测准确率
**测试方法**：预设pending任务 → 用21种不同表达方式测试 → 验证检测准确性
**测试数据集覆盖**：
- 标准表达: "完成了"、"做完了"、"搞定了"（3条）
- 变体表达: "过了"、"交了"、"OK了"、"done"（8条）
- 非完成表达: "明天再做"、"还没做完"、"不做了"（4条）
- 跨任务误触发: "实验完成了"不应触发JUC任务（2条）
**关键发现**（面试素材）：
> 纯规则匹配只能覆盖固定表达，"实验过了"、"实验交了"等变体无法识别。最终改为LLM智能判断，准确率从~60%提升到~95%。
#### 维度3: 情绪识别准确率
**测试方法**：8条带情绪标注的对话 → 验证情绪类型和强度范围
**覆盖**：happy/sad/anxious/excited/calm/frustrated/tired/neutral 全部8种情绪
#### 维度4: 幻觉检测
**测试方法**：注入已知事实 → 验证系统提示包含诚实性规则 → 验证不应编造未提及的信息
**关键规则**（系统提示中）：
- 不要说"我记住了"、"我帮你记着"，除非确实做了
- 如果不确定，就说"我不确定"
### 12.3 记忆巩固机制
**问题**：数据库会无限膨胀，3个月后查询变慢。
**解决方案**：每天凌晨3点自动执行记忆巩固（`scheduler.schedule_daily("memory_consolidation", 3, 0, ...)`）
**巩固内容**：
1. **Summarizer** — 压缩7天前的旧对话为1-2句摘要
2. **Forgetter** — 三策略清理：
   - 过期遗忘（30天前的临时事件）
   - 重复合并（同subject的重复事实）
   - 低置信度清理（confidence < 0.3）
**代码位置**：`src/modes/daemon.py` 第173-189行
### 测评结果（基于98条真实对话数据）

> 测评基于用户真实使用的98条对话、30条事实、11条情绪、51条关联索引，使用数据库只读副本进行，不影响现有数据。

| 维度 | 指标 | 数据集规模 | 结果 |
|------|------|-----------|------|
| 记忆召回准确率 | Recall@5 | 43条（基于22条真实事实的多查询测试） | **100%**（43/43全部召回） |
| 任务完成检测 | 准确率 | 64条（8个真实任务 x 8种表达） | **87.5%**（56/64通过） |
| 情绪识别一致性 | 一致率 | 7条（基于11条真实情绪记录） | **100%**（7/7一致） |
| RAG检索质量 | 成功率 | 25条（15条用户消息+10条事实查询） | **100%**（25/25命中） |
| 信息抽取完整性 | 覆盖率 | 8条（34条用户消息中有可抽取信息的） | **75%**（6/8已抽取） |
| 上下文管理 | 连贯性得分 | 73条对话（2个会话） | **53%**（跨天间隔正常） |

**综合得分**：**85.9%**

**测评方法**：
1. 创建数据库只读副本，不影响生产数据
2. 基于真实事实设计多查询测试（subject查询+关键词查询）
3. 为每个真实任务生成多种完成/未完成表达
4. 匹配情绪记录与时间相近的用户消息验证一致性
5. 使用用户消息关键词测试RAG检索命中率

**各维度说明**：
- **记忆召回100%**：22条真实事实全部能通过subject或关键词正确检索到
- **任务检测87.5%**：8个失败案例主要是边界情况（如"OK"单独出现时的误判）
- **情绪识别100%**：所有情绪记录都有对应的上下文或关键词支持
- **RAG检索100%**：所有查询都能在对话或事实中找到相关内容
- **信息抽取75%**：2条遗漏是因为消息中的可抽取信息较隐晦
- **上下文管理53%**：分数较低是因为跨天对话有自然间隔，属于正常现象


### 12.4 面试话术
**Q: 项目怎么测评的？**
> "建立了4个评估维度：记忆召回准确率（Recall@5，8种事实类型）、任务完成检测（21种表达方式）、情绪识别（8种情绪全覆盖）、幻觉检测（诚实性规则验证）。总共18个测评用例 + 55个单元测试 = 73个测试。测评数据集都是手工标注的真实对话场景，不是合成数据。"
**Q: 测评结果怎么样？**
> "基于98条真实对话数据的综合测评，6个维度共220条测试，综合得分85.9%。记忆召回Recall@5达到100%（43/43），RAG检索成功率100%（25/25），情绪识别一致率100%（7/7），任务完成检测87.5%（56/64），测评使用数据库只读副本，不影响生产数据，所有测试用例都来自真实用户对话。"
**Q: 为什么要做测评？**
> "单元测试验证代码没bug，但不验证系统效果好不好。比如代码跑通了，但Agent回复质量差、记忆召回不准、任务检测漏报——这些单元测试发现不了。测评框架是验证系统行为的：给定输入，输出是否符合预期。这是工程化和demo的区别。"

---

## 十三、设计决策总览（每个模块为什么这样设计）

> 面试官最想听的不是"你用了什么"，而是"你为什么这样选"。本章系统性解释每个关键设计决策的思考过程。

### 13.1 为什么用5层记忆架构，而不是一张大表？

**问题**：对话、事实、情绪、任务、学习记录混在一起会怎样？

**思考过程**：
- 如果一张表存所有数据，查询"用户喜欢什么"需要扫描全部对话记录，O(n) 复杂度
- 不同类型的数据有不同的生命周期：偏好永久保留，事件7天后压缩，情绪只看趋势
- 不同类型的数据有不同的查询模式：事实按subject查，情绪按时间查，任务按日期查

**决策**：按数据语义分层存储
- Layer 0 (conversations): 全量存档，保留原始对话
- Layer 1 (facts): 结构化事实，Upsert语义去重
- Layer 2 (emotions): 情绪时间线，用于趋势分析
- Layer 3 (associations): 关键词索引，加速检索
- Layer 4 (tasks): 任务管理，独立的状态机

**权衡**：分层增加了写入时的复杂度（每条消息要写多个表），但大幅降低了查询复杂度和存储冗余。

### 13.2 为什么Fact用Upsert而不是Insert？

**问题**：用户说了10次"喜欢Python"，存10条还是1条？

**思考过程**：
- 如果用Insert，152条事实中有15条是"实验"，12条是"今日任务"——噪音淹没信号
- 如果用Delete+Insert，会丢失历史版本（confidence变化）
- Upsert（同subject+fact_type更新）：保留最新版本，不重复，不丢历史

**决策**：`save_fact()` 用 INSERT OR UPDATE 语义
```python
# database.py
INSERT INTO facts (subject, fact_type, content, confidence, ...)
ON CONFLICT(subject, fact_type) DO UPDATE SET
    content = excluded.content,
    confidence = excluded.confidence,
    updated_at = excluded.updated_at
```

**效果**：152条 → 约30-40条，prompt噪音大幅减少

### 13.3 为什么Fact分层过滤（稳定 vs 临时）？

**问题**：所有事实都注入prompt会怎样？

**思考过程**：
- "喜欢Python"（preference）是稳定画像，永远有用
- "7月20日去了四川"（event）是临时事件，3个月后就不相关了
- 如果不区分，旧事件会淹没新信息，LLM看到的上下文越来越长但越来越没用

**决策**：
- 稳定画像（preference/opinion/habit/person/goal）：永久保留，全量注入
- 临时事件（event/commitment）：只保留最近7天
- 7天前的旧事件：按subject去重，每个subject只保留最新1条

**权衡**：可能丢失有价值的旧事件，但宁可少给也不要噪音。用户真需要旧信息时，RAG语义检索会找到。

### 13.4 为什么RAG用余弦相似度而不是FAISS？

**问题**：个人助手场景需要向量数据库吗？

**思考过程**：
- FAISS/ChromaDB 是为百万级向量设计的，个人助手的数据量级是<10万条
- SQLite 存储向量（struct.pack float32 BLOB）足够简单，不引入外部依赖
- 全表扫描+余弦相似度在10万条以内是毫秒级，不需要近似搜索

**决策**：SQLite + struct.pack 序列化 + 全表扫描余弦相似度

**权衡**：牺牲了大规模下的查询性能，换取了部署简单（一个SQLite文件搞定所有）和零外部依赖。

### 13.5 为什么Embedding缓存用SQLite而不是Redis？

**问题**：embedding计算很慢，怎么加速？

**思考过程**：
- 每次对话都要计算query的embedding，如果每次都调LLM embed API会很慢
- Redis需要额外部署，增加运维复杂度
- SQLite已经存在，embedding表直接加在memory.db里

**决策**：`EmbeddingCache` — cache-aside模式，miss时计算并存储在SQLite embeddings表
```python
async def get_or_compute(self, entity_id, entity_type, content):
    # 1. 查缓存
    cached = await self._get_from_db(entity_id)
    if cached: return cached
    # 2. 计算embedding
    embedding = await self.llm.embed(content)
    # 3. 存入缓存（struct.pack序列化为BLOB）
    await self._save_to_db(entity_id, entity_type, embedding)
    return embedding
```

**权衡**：SQLite并发性能不如Redis，但个人助手是单用户场景，不需要高并发。

### 13.6 为什么LLM用双后端（Ollama + OpenAI）？

**问题**：为什么不用一个LLM后端？

**思考过程**：
- 聊天需要高质量模型（云端API），但embedding需要本地模型（云端API通常不支持embedding）
- 成本考虑：embedding每次都要调，用本地Ollama免费
- 灵活性：不同任务可以用不同模型（聊天用大模型，抽取用小模型）

**决策**：
- 聊天：`provider` 配置决定用Ollama还是OpenAI（或其他兼容API）
- Embedding：固定用Ollama的nomic-embed-text（本地免费）

**权衡**：增加了配置复杂度，但降低了成本和提高了灵活性。

### 13.7 为什么信息抽取用Fire-and-forget？

**问题**：抽取过程要等它完成再回复用户吗？

**思考过程**：
- 抽取需要额外的LLM调用（~1-2秒），如果等它完成再回复，用户要多等1-2秒
- 抽取失败不应该影响对话回复（降级策略）
- 抽取是异步的后台任务，用户不需要知道它的存在

**决策**：`await` 抽取但在回复之后执行，失败静默跳过
```python
# handler.py - 回复已经返回给用户后
try:
    facts, emotion, topics = await self.extractor.extract(user_msg)
    # 保存到数据库...
except Exception as e:
    logger.warning(f"信息提取失败（不影响回复）: {e}")
```

**权衡**：可能丢失一些记忆（如果抽取失败），但保证了对话的响应速度。

### 13.8 为什么任务检测用正则+LLM混合而不是纯LLM？

**问题**：任务检测应该用规则还是LLM？

**思考过程**：
- "今日任务：A；B；C" 格式固定，正则100%准确，0延迟，0成本
- "实验完成了" 的语义理解需要LLM，正则只能覆盖固定表达
- 纯LLM方案：每次对话都调LLM判断是否在列任务/完成任务，成本高且有幻觉风险

**决策**：
- 任务列表检测：纯正则（`detect_task_list`），格式固定，不需要LLM
- 任务完成检测：LLM判断（`detect_task_completion`），需要语义理解

**权衡**：混合方案比纯LLM更可靠（正则部分100%准确），比纯规则更灵活（LLM部分能理解自然语言）。

### 13.9 为什么主动干预用规则引擎而不是LLM？

**问题**：主动关怀应该用LLM生成还是规则触发？

**思考过程**：
- 主动干预需要定时执行（每4小时检查一次），如果每次都调LLM生成关怀消息，成本很高
- 规则简单明确：24h内负面情绪>60% → 发关怀，不需要LLM判断
- 规则有冷却机制（12h内不重复），LLM很难保证不重复打扰

**决策**：4条硬编码规则 + 冷却时间控制

**权衡**：规则不够灵活（新增场景需要改代码），但对于固定的关怀场景（情绪、沉默、屏幕时间），规则足够且更可靠。

### 13.10 为什么流式输出用SSE而不是WebSocket？

**问题**：流式对话应该用什么协议？

**思考过程**：
- SSE是单向的（服务器→客户端），适合对话场景（用户发一条，AI回多条）
- WebSocket是双向的，更复杂，但对话场景不需要双向实时通信
- SSE可以直接用fetch API + ReadableStream，前端实现更简单
- SSE支持自动重连，WebSocket需要手动处理

**决策**：`POST /api/chat/stream` 返回 `StreamingResponse(media_type="text/event-stream")`

**权衡**：SSE不支持二进制数据，但对话场景只需要文本，够用。

### 13.11 为什么定时调度器自己实现而不是用APScheduler？

**问题**：需要定时任务，用现成的库还是自己写？

**思考过程**：
- APScheduler功能强大但依赖重（需要额外安装，配置复杂）
- 项目只需要两种调度：每日定时 + 间隔执行
- 自己实现只需60行代码，且支持跨天检测（APScheduler需要额外配置）

**决策**：60行的CronScheduler，支持daily + interval两种模式 + 跨天回调

**权衡**：功能有限（不支持cron表达式、不支持持久化），但对于个人助手场景够用。

### 13.12 为什么测试分3个阶段而不是一次性写完？

**问题**：55个测试应该一次性写还是分阶段？

**思考过程**：
- 一次性写完所有测试，可能写了很多用不到的测试
- 分阶段：先写核心功能的测试（Phase1: 记忆+对话），确保基础稳固
- 每阶段发现问题就修，避免bug累积

**决策**：
- Phase 1: 核心记忆+对话 (25 tests)
- Phase 2: 情绪追踪+日报+语义检索 (33 tests)
- Phase 3: 感知+主动干预+服务器部署 (55 tests)

**权衡**：分阶段需要更多时间（每次重构都要更新测试），但保证了每个阶段的质量。

### 13.13 设计决策总结表

| 模块 | 决策 | 替代方案 | 为什么选这个 |
|------|------|---------|-------------|
| 记忆架构 | 5层分表 | 一张大表 | 查询效率+生命周期管理 |
| Fact存储 | Upsert | Insert | 去重+避免噪音 |
| Fact过滤 | 稳定/临时分层 | 全量注入 | 减少prompt噪音 |
| 向量存储 | SQLite BLOB | FAISS/ChromaDB | 零依赖+部署简单 |
| Embedding缓存 | SQLite cache-aside | Redis | 零依赖+单用户够用 |
| LLM后端 | Ollama+OpenAI双后端 | 单后端 | 成本+灵活性 |
| 信息抽取 | Fire-and-forget | 同步等待 | 响应速度+降级策略 |
| 学习识别 | LLM智能判断 | 关键词匹配 | 覆盖面广+0额外延迟（合并到抽取prompt） |
| 任务检测 | 正则+LLM混合 | 纯LLM | 可靠性+成本 |
| 主动干预 | 规则引擎 | LLM生成 | 成本+可预测性 |
| 流式输出 | SSE | WebSocket | 简单+够用 |
| 定时调度 | 自实现60行 | APScheduler | 轻量+够用 |
| 测试 | 3阶段迭代 | 一次性写完 | 质量保证+渐进式 |
| LLM容错 | 兜底+熔断+缓存三层防护 | 仅重试 | 可用性+降级+性能 |

## 十四、多通道配置详解

### 14.1 微信接入 (iLink Bot)

**原理**：通过iLink Bot API实现微信消息收发，无需企业微信。

**配置步骤**：
1. 注册iLink账号：https://ilink.im/
2. 获取API Token
3. 扫码登录微信机器人
4. 配置到config.yaml

**config.yaml配置**：
```yaml
wechat:
  enabled: true
  api_token: "your-ilink-token"
  polling_interval: 5
```

**消息流程**：用户微信 → iLink API轮询 → CommandDispatcher → 回复 → iLink发送

**代码位置**：`src/wechat/connection.py`

---

### 14.2 飞书接入 (Webhook)

**原理**：飞书开放平台事件订阅，Webhook实时推送。

**配置步骤**：
1. 访问 https://open.feishu.cn/ 创建应用
2. 获取 app_id 和 app_secret
3. 事件订阅：请求地址 = http://服务器IP:8088/feishu/webhook
4. 权限：im:message (收消息) + im:message:send (发消息)
5. 发布应用

**config.yaml配置**：
```yaml
feishu:
  enabled: true
  app_id: "cli_xxxxxxxxxxxx"
  app_secret: "xxxxxxxxxxxxxxxx"
  owner_id: "ou_xxxxxxxxxxxx"
```

**消息流程**：用户飞书 → 飞书服务器 → Webhook → /feishu/webhook → CommandDispatcher → 回复

**代码位置**：`src/feishu/connection.py`

---

### 14.3 Web接入 (SSE)

**访问**：http://服务器IP:8088/chat

**技术**：FastAPI + SSE流式输出，前端原生HTML/JS

**代码位置**：`src/api/chat.py`

---

### 14.4 三通道共享记忆

**核心**：所有通道 → CommandDispatcher.dispatch() → 同一个MemoryDatabase

```
微信 ──┐
飞书 ──┼→ CommandDispatcher → Handler → memory.db
Web  ──┘
```

**效果**：微信聊的，飞书也能看到，记忆完全共享。
